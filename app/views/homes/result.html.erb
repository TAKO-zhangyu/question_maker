<p>こんにちは、<%= @name %>です。</p>
<p><%= @action %>を実行すると、<%= @error %>というエラーになる問題で困っています。</p>
私の行った手順は以下です。
<p><%= @step %></p>
すると、以下のような結果になりました。
<p><%= @error_code %></p>
<p>私は、<%= @expect %>になると思いました。</p>
<p>なぜなら、<%= @reference %>には、以下のように書かれているからです。</p>
原因を確かめるため、以下のようなテストを行ってみましたが、
問題の解決には至りませんでした。
<p><%= @try %></p>
<p><%= @result %></p>
<p>OS：<%= @user_pc %></p>
<p>開発環境：<%= @environment %></p>
<p>エディター：<%= @editor %></p>
<p>使用言語：<%= @programming_lang %></p>
<p>GitHubリンク：<%= @github_link %></p>
<p><%= @signature %></p>
